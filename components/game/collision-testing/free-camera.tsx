'use client'

// Stub component for free camera
export function FreeCamera({ enabled, moveSpeed, zoomSpeed }: { enabled?: boolean; moveSpeed?: number; zoomSpeed?: number }) {
  return null
}

export default FreeCamera

